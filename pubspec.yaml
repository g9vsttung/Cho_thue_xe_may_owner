name: chothuexemay_owner
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  #State Manager
  provider: 6.0.0

  #Goole Signin
  google_sign_in: ^5.1.1
  
  #firebase
  firebase_core: ^1.6.0
  firebase_auth: ^3.1.1
  firebase_storage: ^10.0.5
  
  #Google Icon
  font_awesome_flutter: ^9.1.0
  
  http: ^0.13.3

  cupertino_icons: ^1.0.2
  image_picker: ^0.8.4
  jwt_decode: ^0.3.1
  shared_preferences: ^2.0.8
  
  #flutter_session: ^0.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


flutter:
  assets:
    - assets/images/
    - assets/icons/

  uses-material-design: true

